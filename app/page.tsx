export default function HomePage() {
  return (
    <main className="container mx-auto p-8">
      <h1 className="text-4xl font-bold mb-8">Nossos Produtos</h1>

      {/* Aqui é onde vamos listar nossos produtos em breve.
        O código da API route que criamos em 'app/api/products/route.js' 
        será chamado por esta página.
      */}
    </main>
  );
}
